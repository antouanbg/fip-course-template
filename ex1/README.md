# Упражнение 1 · Frontend ↔ Backend с HTML + JavaScript (с AI агент)

| | |
|---|---|
| **Технологии** | HTML, vanilla JavaScript (`fetch`, `async/await`), JSON, HTTP |
| **Среда** | Codespaces: `cd ex1 && npm start` → порт 8080 (или десен клик на `index.html` → *Open with Live Server*) |
| **AI агент** | Gemini CLI (`gemini` в терминала) / Copilot Chat / уеб чат — виж [README](../README.md) и [PROMPTS.md](PROMPTS.md) |
| **Време** | 90 мин · **Self-check:** `node checks/ex1.mjs` · **Предаване:** `git tag ex1 && git push origin ex1` |

## Цел

- Да видите нагледно **frontend / backend / база данни** и как браузърът говори с отдалечен сървър по HTTP.
- Да четете JSON отговор и да го показвате с JavaScript; да различавате HTTP грешка от мрежова грешка.
- Да работите с агент **като reviewer**: формулирате задача → получавате код → проверявате го на контролна точка → можете да го обясните.

## Какво има в кода

`index.html`: бутонът **Get Joke** прави `fetch()` (HTTP GET) към `https://api.chucknorris.io/jokes/random`, получава JSON и показва полето `value`.

| Слой | Тук | Роля |
|---|---|---|
| **Frontend** | `index.html` в браузъра ви | UI + логика на клиента |
| **Backend** | `api.chucknorris.io` | Отговаря на HTTP заявки с JSON |
| **Database** | зад API-то, невидима за нас | Съхранява данните |

---

## Част A · На ръка, без агент (10 мин)

Агентът **не пише** тази част (така е записано и в `AGENTS.md`).

- [ ] **A1** Името и ФН ви в `<header>`.
- [ ] **A2** Изображение (`https://picsum.photos/200`) и линк към документацията на API-то под отговора.
- [ ] **A3** `<footer>` с текущата година.
- [ ] **A4** DevTools → **Network**, натиснете бутона. Попълнете таблицата в края на този файл: метод, status, `Content-Type`, размер.

## Част B · С агент (45 мин) — всяка задача = запис в `AGENT_LOG.md`

- [ ] **B1 Обясни.** Поискайте обяснение на `<script>` ред по ред. После **със свои думи**, в коментар в кода (3–4 изречения): какво връща `fetch()`, защо има два `await`, какво се случва при клик.
- [ ] **B2 Обработка на грешки.** Поискайте `try/catch` + проверка на `response.ok` + съобщение за потребителя (клас `.error`). **Контролна точка:** преди да приемете кода, тествайте трите сценария и попълнете таблицата в края:
  1. грешен path (`/jokes/randomXYZ`) → какъв status? влиза ли се в `catch`?
  2. грешен домейн (`api.chucknorris.invalid`) → какво хвърля `fetch`?
  3. DevTools → Network → **Offline** → какво вижда потребителят?
- [ ] **B3 UX.** Докато заявката тече: бутонът е `disabled`, текст „Loading…". След отговор: шегата се добавя в `<ul id="history">` с последните 5, най-новата отгоре. `finally` връща бутона.
- [ ] **B4 Търсене.** Поле + бутон **Search** → `…/jokes/search?query=<текст>`. Отговор: `{ total, result: [...] }`. Покажете `total` и списък. Обработете празен вход, `total = 0`, специални символи (`encodeURIComponent`). API-то връща **400** при под 3 символа — проверете го сами.

**Преди да приемете код от агента** — чеклистът в [PROMPTS.md](PROMPTS.md): работи ли? прави ли *само* поисканото? без библиотеки? тествахте ли грешния път? можете ли да обясните всеки ред?

## Част C · Бонус — Spec first (ако остане време / за вкъщи)

Напишете 5–8 реда спецификация в раздела *Prompt experiment* по-долу (цел, endpoint, форма на JSON — **проверена в браузъра**, UI, грешки, ограничения) и **едва след това** я дайте на агента. Проверете резултата срещу спецификацията. API по избор:
- `https://dog.ceo/api/breeds/image/random` → `{ message: "<url>", status }` — покажете изображението.
- `https://api.adviceslip.com/advice` → `{ slip: { id, advice } }` — вложен обект; API-то кешира — как ще получите нов съвет при всеки клик?

## Разбор в края (10 мин) — по **вашия** код

- Кое е frontend, кое backend? Какво не виждаме?
- `fetch` хвърля ли exception при 404? Защо е нужен `response.ok`?
- Какво прави `await` и защо са два? Какво става без него?
- Защо `encodeURIComponent`? Защо `textContent`, а не `innerHTML`?
- Покажете prompt, който **не** даде добър резултат — какво му липсваше?

---

# Вашите бележки (попълват се; self-check-ът ги проверява)

## How to run
<!-- Точната команда, с която преподавателят пуска страницата ви. -->

## A4 · Network
<!-- Метод | URL | Status | Content-Type | Размер | Време -->

| Метод | URL | Status | Content-Type | Размер | Време |
|---|---|---|---|---|---|

## B2 · Error scenarios
<!-- За всеки сценарий: какво връща/хвърля fetch, влиза ли се в catch, какво вижда потребителят. -->

| Сценарий | Какво връща/хвърля `fetch` | В `catch`? | Потребителят вижда |
|---|---|---|---|
| Грешен path | | | |
| Грешен домейн | | | |
| Offline | | | |

## Prompt experiment
<!-- Три изречения: вземете prompt-а от B2, махнете ограниченията ("само vanilla JS, само <script> блока, запази имената") и го пуснете отново в нов чат. Какво се промени в резултата и защо според вас? -->

## Reflection
<!-- Кратко, със свои думи. -->
1. Frontend / backend / база данни в тази страница:
2. HTTP грешка (404/500) vs. мрежова грешка за `fetch()` — разлика и как се хващат:
3. Едно нещо, което агентът сгреши / предложи излишно / аз промених — и защо:
